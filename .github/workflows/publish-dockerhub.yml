name: Publish Docker Hub

on:
  push:
    branches:
      - main
      - dev
      - 'dev-*'
    tags:
      - '*'
  workflow_dispatch:

jobs:
  wait-for-checks:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: read
    steps:
      - name: Wait for lint
        uses: fountainhead/action-wait-for-check@v1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: lint
          ref: ${{ github.sha }}
          timeoutSeconds: 900
          intervalSeconds: 15

      - name: Wait for the integration suite
        uses: fountainhead/action-wait-for-check@v1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: integration
          ref: ${{ github.sha }}
          timeoutSeconds: 900
          intervalSeconds: 15

      - name: Wait for the image build
        uses: fountainhead/action-wait-for-check@v1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: docker-build
          ref: ${{ github.sha }}
          timeoutSeconds: 900
          intervalSeconds: 15

  publish:
    needs: wait-for-checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      DOCKERHUB_USER: ${{ vars.DOCKERHUB_USER }}
      DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
      DOCKERHUB_IMAGE: ${{ vars.DOCKERHUB_IMAGE }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate configuration
        run: |
          set -euo pipefail
          for var in DOCKERHUB_USER DOCKERHUB_PASSWORD DOCKERHUB_IMAGE; do
            if [ -z "${!var}" ]; then
              echo "Missing value for $var" >&2
              exit 1
            fi
          done

      - name: Determine tags
        run: |
          set -euo pipefail
          ref="${GITHUB_REF}"
          ref_name="${GITHUB_REF_NAME}"
          sha_short="$(printf '%s' "${GITHUB_SHA}" | cut -c1-7)"
          if [[ "${ref}" == refs/tags/* ]]; then
            suffix="${ref#refs/tags/}"
          elif [[ "${ref_name}" == "main" ]]; then
            suffix="latest"
          else
            slug="$(printf '%s' "${ref_name}" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g' | sed -E 's/^-+|-+$//g')"
            [[ -z "${slug}" ]] && slug="sha-${sha_short}"
            suffix="${slug}"
          fi
          echo "BASE_TAG=docker.io/${DOCKERHUB_IMAGE}:${suffix}" >> "${GITHUB_ENV}"
          echo "SHA_TAG=docker.io/${DOCKERHUB_IMAGE}:sha-${sha_short}" >> "${GITHUB_ENV}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ env.DOCKERHUB_PASSWORD }}

      - name: Build and push a multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.BASE_TAG }}
            ${{ env.SHA_TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
